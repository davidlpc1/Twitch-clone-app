import styled from 'styled-components/native';
import colors from '../../styles/colors';

export const DiscoverContainer = styled.SafeAreaView`
    background: ${colors.primary};
    flex:1;
    padding:14px;
`;  

export const ContainerCategory = styled.View`
    padding-left:14px;
`;
